export enum Os {
	Linux = 0,
	Darwin,
	Windows,
	FreeBSD,
}

export enum ChartType {
	Memory,
	Disk,
	Network,
	CPU,
}

export enum DataUnit {
	Bytes,
	Bits,
}

export enum TemperatureUnit {
	Celsius,
	Fahrenheit,
}
